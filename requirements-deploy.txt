# Deploy requirements — install on the DEVELOPER laptop only
#
# These are only needed to run scripts/deploy.py (EC2 start/SSH/git pull).
# Do NOT install on the EC2 instance itself.
#
# Usage:
#   pip install -r requirements-deploy.txt
#   make install-deploy

# AWS SDK — start/stop EC2, describe instances
boto3==1.40.4
botocore==1.40.4
s3transfer==0.13.1
jmespath==1.0.1

# SSH client — remote command execution on EC2
paramiko==4.0.0
PyNaCl==1.5.0
bcrypt==4.3.0
