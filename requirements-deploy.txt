# Deploy requirements — install on the DEVELOPER laptop only
#
# scripts/deploy.py needs NONE of these — it uses plain ssh/scp against the
# production server (physical machine via Tailscale, see
# configs/server_metadata.yaml). These packages are only needed to run
# scripts/service_stop.py (legacy EC2 start/stop automation).
# Do NOT install on the production server itself.
#
# Usage:
#   pip install -r requirements-deploy.txt
#   make install-deploy

# AWS SDK — EC2 start/stop for scripts/service_stop.py
boto3==1.40.4
botocore==1.40.4
s3transfer==0.13.1
jmespath==1.0.1

# SSH client — remote command execution for scripts/service_stop.py
paramiko==4.0.0
PyNaCl==1.5.0
bcrypt==4.3.0
